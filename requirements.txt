fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.11
pydantic-settings>=2.7
email-validator>=2.2
motor>=3.6
redis>=5.2
python-jose[cryptography]>=3.3
bcrypt>=4.2
python-multipart>=0.0.20
structlog>=24.4
cryptography>=43   # explicit — used directly for per-user LLM key encryption (Fernet)
httpx>=0.28         # also used at runtime — calling providers' /models endpoints

# ingestion (Phase 2) — no boto3/arq: object storage + background workers are
# deferred, see plan/backend/phase-2b-storage-and-workers.md
pypdf>=5.1
python-docx>=1.1

# vector pipeline (Phase 3)
pinecone>=5.4
tiktoken>=0.8
openai>=1.55
tenacity>=9.0

# dev / test
pytest>=8.3
pytest-asyncio>=0.25
fakeredis>=2.26
mongomock-motor>=0.0.34
ruff>=0.8
